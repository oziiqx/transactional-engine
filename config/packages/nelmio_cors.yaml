nelmio_cors:
    defaults:
        origin_regex: true
        allow_origin: ['%env(CORS_ALLOW_ORIGIN)%']
        allow_methods: ['GET', 'OPTIONS', 'POST', 'PUT', 'PATCH', 'DELETE']
        allow_headers: ['Content-Type', 'Authorization', 'X-Api-Key', 'X-Signature', 'X-Idempotency-Key']
        expose_headers: ['Link', 'X-Request-Id', 'Retry-After']
        max_age: 3600
    paths:
        '^/api/':
            allow_credentials: true
